# If running bash
if [ -n "$BASH_VERSION" ]; then
    # include .bashrc if it exists
    if [ -f "$HOME/.bashrc" ]; then
	. "$HOME/.bashrc"
    fi
fi
# Ensure kiosk symlinks are searched first
export PATH="$HOME"/.local/bin:$PATH
# System info display
ffetch
